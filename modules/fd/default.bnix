#lang beagle/nix

(ns default)

(module (params config lib pkgs ...)
        {:options.myConfig.modules.fd.enable
         (lib.mkEnableOption "fd file finder")
         :config
         (lib.mkIf config.myConfig.modules.fd.enable
                   {:environment.systemPackages (with pkgs [fd])})})
