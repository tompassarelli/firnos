#lang beagle/nix

(ns default)

(module [config lib pkgs ...]
        {:options.myConfig.modules.brightnessctl.enable
         (lib.mkEnableOption "screen brightness control")
         :config
         (lib.mkIf config.myConfig.modules.brightnessctl.enable
                   {:environment.systemPackages (with pkgs [brightnessctl])})})
