#lang beagle/nix

(ns default)

(nix/module [config lib pkgs ...]
  {:tags [gaming]
   :options.myConfig.modules.wowup.enable
    (lib.mkEnableOption "WowUp-CF addon manager")
   :config
    (lib.mkIf config.myConfig.modules.wowup.enable
      {:environment.systemPackages [pkgs.wowup-cf]})})
