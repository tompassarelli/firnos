#lang beagle/nix

(ns default)

(nix/module [config lib pkgs ...]
  {:tags-opt-in [browsers]
   :options.myConfig.modules.gjoa.enable
    (lib.mkEnableOption
      "Gjoa — a Firefox fork. Wrapped via wrapFirefox; appears in launchers/drun.")
   :options.myConfig.modules.gjoa.default
    (lib.mkOption
      {:type        lib.types.bool
       :default     false
       :description "Set Gjoa as the default browser via MIME types"})
   :imports [(p "./gjoa.nix")]})
