#lang beagle/nix

(ns default)

(module [config lib pkgs ...]
  '{options.myConfig.modules.parted.enable
     (lib.mkEnableOption "disk partitioning tool")
    config
     (lib.mkIf config.myConfig.modules.parted.enable
       '{environment.systemPackages (with pkgs [parted])})})
