#lang beagle/nix

(ns default)

(nix/module [config lib pkgs ...]
  {:options.myConfig.modules.nix-ld.enable
    (lib.mkEnableOption "nix-ld dynamic library shim")
   :config
    (lib.mkIf config.myConfig.modules.nix-ld.enable
      {:programs.nix-ld.enable true})})
