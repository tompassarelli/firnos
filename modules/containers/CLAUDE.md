# claude-sandbox container

Stub file referenced by `claude-sandbox.nix` via `${./CLAUDE.md}`. The file
gets copied into the container's `/work/CLAUDE.md` to provide Claude Code
with project context at sandbox startup.

This stub is a placeholder; replace with real project context as needed.
