#lang beagle/nix

(ns default)

(nix/module [config lib pkgs ...]
  {:options.myConfig.modules.slurp.enable
    (lib.mkEnableOption "Wayland region selector")
   :config
    (lib.mkIf config.myConfig.modules.slurp.enable
      {:environment.systemPackages (nix/with pkgs [slurp])})})
