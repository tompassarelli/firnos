#lang beagle/nix

(ns default)

(nix/module [config lib pkgs ...]
  {:tags [rust]
   :options.myConfig.modules.rustc.enable (lib.mkEnableOption "Rust compiler")
   :config
    (lib.mkIf config.myConfig.modules.rustc.enable
      {:environment.systemPackages [pkgs.unstable.rustc]})})
