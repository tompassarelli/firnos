#lang beagle/nix

(ns default)

(nix/module [config lib pkgs ...]
  {:tags [database]
   :options.myConfig.modules.sqlite.enable
    (lib.mkEnableOption "SQLite database")
   :config
    (lib.mkIf config.myConfig.modules.sqlite.enable
      {:environment.systemPackages [pkgs.sqlite]})})
