#lang beagle/nix

(ns default)

(nix/module [config lib pkgs ...]
  {:tags [media]
   :options.myConfig.modules.mpv.enable
    (lib.mkEnableOption "mpv media player")
   :config
    (lib.mkIf config.myConfig.modules.mpv.enable
      {:environment.systemPackages (nix/with pkgs [mpv])})})
