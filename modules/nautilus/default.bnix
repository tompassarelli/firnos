#lang beagle/nix

(ns default)

(nix/module [config lib pkgs ...]
  {:options.myConfig.modules.nautilus.enable
    (lib.mkEnableOption "Nautilus file manager")
   :config
    (lib.mkIf config.myConfig.modules.nautilus.enable
      {:environment.systemPackages (nix/with pkgs [nautilus])})})
