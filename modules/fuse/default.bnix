#lang beagle/nix

(ns default)

(nix/module [config lib pkgs ...]
  {:options.myConfig.modules.fuse.enable
    (lib.mkEnableOption "FUSE filesystem support")
   :config
    (lib.mkIf config.myConfig.modules.fuse.enable
      {:environment.systemPackages (nix/with pkgs [fuse])})})
