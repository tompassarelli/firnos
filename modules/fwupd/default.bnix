#lang beagle/nix

(ns default)

(nix/module [config lib pkgs ...]
  {:options.myConfig.modules.fwupd.enable
    (lib.mkEnableOption "fwupd firmware updater")
   :config
    (lib.mkIf config.myConfig.modules.fwupd.enable
      {:environment.systemPackages (nix/with pkgs [fwupd])})})
