#lang beagle/nix
(ns modules.godot)

(module [config lib pkgs]
  {:options.myConfig.modules.godot.enable
     (lib/mkEnableOption "Godot game engine")

   :config
     (lib/mkIf config.myConfig.modules.godot.enable
       {:environment.systemPackages (with pkgs [pkgs.unstable.godot_4])})})
