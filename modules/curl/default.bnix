#lang beagle/nix

(ns default)

(nix/module [config lib pkgs ...]
  {:options.myConfig.modules.curl.enable
    (lib.mkEnableOption "curl HTTP client")
   :config
    (lib.mkIf config.myConfig.modules.curl.enable
      {:environment.systemPackages (nix/with pkgs [curl])})})
