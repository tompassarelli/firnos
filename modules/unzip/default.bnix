#lang beagle/nix

(ns default)

(nix/module [config lib pkgs ...]
  {:tags [cli-tools]
   :options.myConfig.modules.unzip.enable
    (lib.mkEnableOption "unzip archive tool")
   :config
    (lib.mkIf config.myConfig.modules.unzip.enable
      {:environment.systemPackages [pkgs.unzip]})})
