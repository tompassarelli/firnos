#lang beagle/nix

(ns default)

(nix/module [config lib pkgs ...]
  {:tags [cli-tools]
   :options.myConfig.modules.wget.enable
    (lib.mkEnableOption "wget download tool")
   :config
    (lib.mkIf config.myConfig.modules.wget.enable
      {:environment.systemPackages [pkgs.wget]})})
