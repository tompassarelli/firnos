#lang beagle/nix

(ns default)

(nix/module [config lib pkgs ...]
  {:tags [development]
   :options.myConfig.modules.vim.enable (lib.mkEnableOption "Vim text editor")
   :config
    (lib.mkIf config.myConfig.modules.vim.enable
      {:environment.systemPackages (nix/with pkgs [vim])})})
