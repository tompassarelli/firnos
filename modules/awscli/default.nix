{ config, flakeRoot, lib, pkgs, ... }:
{ "config" = (lib."mkIf" (config."myConfig"."modules"."awscli"."enable") ({ "environment" = { "systemPackages" = [ (pkgs."awscli2") ]; }; "sops" = { "secrets" = { "aws-access-key-id" = { "owner" = config."myConfig"."modules"."users"."username"; "sopsFile" = config."myConfig"."modules"."awscli"."sopsFile"; }; "aws-secret-access-key" = { "owner" = config."myConfig"."modules"."users"."username"; "sopsFile" = config."myConfig"."modules"."awscli"."sopsFile"; }; }; "templates" = { "aws-config" = { "content" = "[default]\nregion = us-east-2\noutput = json\n\n"; "owner" = config."myConfig"."modules"."users"."username"; }; "aws-credentials" = { "content" = (((("[default]\naws_access_key_id = " + config."sops"."placeholder"."aws-access-key-id") + "\naws_secret_access_key = ") + config."sops"."placeholder"."aws-secret-access-key") + "\n\n"); "owner" = config."myConfig"."modules"."users"."username"; }; }; }; })); "options" = { "myConfig" = { "modules" = { "awscli" = { "enable" = (lib."mkEnableOption" ("awscli")); "sopsFile" = (lib."mkOption" ({ "default" = (("" + flakeRoot) + "/secrets/aws.yaml"); "description" = "sops file holding the AWS credentials (aws-access-key-id, aws-secret-access-key)"; "type" = lib."types"."str"; })); }; }; }; }; }
