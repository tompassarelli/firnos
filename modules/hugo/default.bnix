#lang beagle/nix

(ns default)

(module (params config lib pkgs ...)
        {:options.myConfig.modules.hugo.enable
         (lib.mkEnableOption "Hugo static site generator")
         :config
         (lib.mkIf config.myConfig.modules.hugo.enable
                   {:environment.systemPackages (with pkgs [hugo])})})
