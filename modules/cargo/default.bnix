#lang beagle/nix
(ns modules.cargo)

(module [config lib pkgs]
  {:options.myConfig.modules.cargo.enable
     (lib/mkEnableOption "Rust package manager")

   :config
     (lib/mkIf config.myConfig.modules.cargo.enable
       {:environment.systemPackages (with-do pkgs [pkgs.unstable.cargo])})})
