#lang beagle/nix

(ns default)

(nix/module [config lib pkgs ...]
  {:tags [desktop]
   :options.myConfig.modules.grim.enable
    (lib.mkEnableOption "Grim screenshot tool")
   :config
    (lib.mkIf config.myConfig.modules.grim.enable
      {:environment.systemPackages (nix/with pkgs [grim])})})
