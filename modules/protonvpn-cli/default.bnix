#lang beagle/nix

(ns default)

(nix/module [config lib pkgs ...]
  {:tags [vpn]
   :options.myConfig.modules.protonvpn-cli.enable
    (lib.mkEnableOption "ProtonVPN CLI client")
   :config
    (lib.mkIf config.myConfig.modules.protonvpn-cli.enable
      {:environment.systemPackages (nix/with pkgs [protonvpn-cli])})})
